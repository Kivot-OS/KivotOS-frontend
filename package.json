{
  "name": "kivotos-repo-worker",
  "version": "1.0.0",
  "private": true,
  "description": "Cloudflare Worker — dynamic index page cho APT repo KivotOS lưu trên R2",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "tail": "wrangler tail",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "wrangler": "^4.0.0",
    "typescript": "^5.6.0",
    "@cloudflare/workers-types": "^4.20250101.0"
  }
}
